 ---
 - hosts: all

   tasks:

    - name: install/update essential software
      cron: name="install/update essential software"
            minute="5"
      apt: pkg={{item}} state=latest

      with_items:

       - nmap
       - wget
       - vim
       - open-vm-tools
